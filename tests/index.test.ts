import { play } from "../src/bowling";
import "jest";

describe("Happy path tests", () => {
  test("Test high and low scores", () => {
    // expect().toBe();
  });
  test("Calculate a variety of more normal scores", () => {});
});

describe("Test edge cases", () => {
  test("Dont allow negatives (in a variety of postions) ", () => {});

  test("Dont allow values greater than 10", () => {});

  test("Don't allow characters", () => {});
});
